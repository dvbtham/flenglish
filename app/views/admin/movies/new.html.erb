<div id="page-wrapper">
  <div class="container-fluid">
    <div class="row">
      <div class="col-lg-12">
        <h1 class="page-header"><%= t :create %></div>
      </div>
      <div><%= render "shared/flash" %></div>
      <%= form_for [:admin, @movie] do |f| %>
        <%= render partial: "admin/movies/form", locals: {f: f} %>
      <% end %>
    </div>
  </div>
</div>
