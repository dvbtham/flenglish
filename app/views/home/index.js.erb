$('#feature-movies')
  .html('<%= j render(partial: "feature_movie",\
    collection: @feature_movies, as: :movie) %>');
$("#movie-paging").html('<%= will_paginate @feature_movies %>');
$('.pagination a').attr('data-remote', 'true');
